(()=>{const tabs=document.querySelectorAll('[data-qrx-tab]');tabs.forEach(btn=>btn.addEventListener('click',()=>{tabs.forEach(x=>x.classList.remove('active'));document.querySelectorAll('[data-qrx-pane]').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.querySelector(`[data-qrx-pane="${btn.dataset.qrxTab}"]`)?.classList.add('active')}));const iframe=document.querySelector('#qrx-preview');let timer;function refresh(){if(!iframe)return;clearTimeout(timer);timer=setTimeout(()=>{const u=new URL(iframe.src,location.href);u.searchParams.set('preview_ts',Date.now());iframe.src=u.toString()},500)}document.querySelectorAll('[data-live]').forEach(el=>el.addEventListener(el.type==='color'?'input':'change',refresh));document.querySelectorAll('[data-live-text]').forEach(el=>el.addEventListener('input',refresh));const list=document.querySelector('#qrx-sortable');if(list){let dragging=null;list.querySelectorAll('li').forEach(item=>{item.draggable=true;item.addEventListener('dragstart',()=>{dragging=item;item.style.opacity='.5'});item.addEventListener('dragend',()=>{item.style.opacity='';dragging=null;syncOrder()});item.addEventListener('dragover',e=>{e.preventDefault();if(!dragging||dragging===item)return;const r=item.getBoundingClientRect();list.insertBefore(dragging,e.clientY<r.top+r.height/2?item:item.nextSibling)})});function syncOrder(){document.querySelector('#qr_section_order').value=[...list.querySelectorAll('li')].map(x=>x.dataset.key).join(',')}}})();
