(()=>{const box=document.getElementById('sortable');if(!box)return;let dragged=null;box.querySelectorAll('.sortable-item').forEach(item=>{item.addEventListener('dragstart',()=>{dragged=item;item.classList.add('dragging')});item.addEventListener('dragend',()=>{item.classList.remove('dragging');dragged=null});item.addEventListener('dragover',e=>{e.preventDefault();if(!dragged||dragged===item)return;const rect=item.getBoundingClientRect();box.insertBefore(dragged,e.clientY<rect.top+rect.height/2?item:item.nextSibling)})})})();
